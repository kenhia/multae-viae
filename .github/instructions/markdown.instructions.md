---
applyTo: "**/*.md"
---

# Markdown Formatting Rules

- When consecutive lines should render as separate lines (not a merged paragraph), end each line (except the last) with **two trailing spaces** to produce a markdown soft line break.
- This commonly applies to key-value metadata lines like `**Key**: value` that appear on consecutive lines without blank lines between them.
